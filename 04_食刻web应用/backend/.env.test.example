# 测试专用环境变量（可选；不会覆盖进程中已有变量）
# 复制为 .env.test 后按需修改

# local | production
TEST_ENV=local

# API 根地址（local 默认 127.0.0.1:8000；production 默认公网）
TEST_BASE_URL=http://127.0.0.1:8000

# 前端首页（留空则根据 BASE_URL 推断）
# TEST_HOME_URL=http://127.0.0.1

# 单次请求超时（秒）
TEST_TIMEOUT=30
